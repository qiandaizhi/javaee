<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context
                           http://www.springframework.org/schema/context/spring-context.xsd
                           http://www.springframework.org/schema/mvc
                           http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 只扫描Controller -->
    <context:component-scan base-package="com.example.dingcan.controller"/>

    <!-- 开启Spring MVC注解驱动，它会自动注册处理JSON的核心组件 -->
    <mvc:annotation-driven/>

    <!--
      最终修正: 启用默认Servlet处理器。
      这个标签的作用是告诉Spring MVC，如果一个请求没有被任何Controller处理，
      就把它交还给Tomcat的默认Servlet处理，从而能够正确访问到静态资源（如index.html）。
    -->
    <mvc:default-servlet-handler/>

    <!-- CORS 跨域配置 -->
    <mvc:cors>
        <mvc:mapping path="/api/**"
                     allowed-origins="http://localhost:8080, http://localhost:8081, http://localhost:5173"
                     allowed-methods="GET, POST, PUT, DELETE, OPTIONS"
                     allow-credentials="true"
                     max-age="3600"/>
    </mvc:cors>
</beans>
