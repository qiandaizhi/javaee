<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dingcan.mapper.DishMapper">

    <select id="selectAllOnSale" resultType="com.example.dingcan.pojo.Dish">
        SELECT
            id, category_id as categoryId, name, description, price, image_url as imageUrl, status
        FROM
            dishes
        WHERE
            status = 1
        ORDER BY id DESC
    </select>

    <select id="findById" resultType="com.example.dingcan.pojo.Dish">
        SELECT
            id, category_id as categoryId, name, description, price, image_url as imageUrl, status
        FROM
            dishes
        WHERE
            id = #{id}
    </select>

    <select id="selectOnSaleByNameContaining" resultType="com.example.dingcan.pojo.Dish">
        SELECT
            id, category_id as categoryId, name, description, price, image_url as imageUrl, status
        FROM
            dishes
        WHERE
            status = 1 AND name LIKE CONCAT('%', #{name}, '%')
        ORDER BY
            id DESC
    </select>

</mapper>